{
   "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["api-types"],
      "outputs": ["dist/**"],
      "inputs": ["src/**"]
    }
  }
}